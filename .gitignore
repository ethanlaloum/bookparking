.jp-way/project.json
node_modules/
.DS_Store

# Skills Claude Code installés localement (tiers, hors produit)
.claude/skills/
skills-lock.json
dist/

# graphify — graphe de connaissance généré localement
graphify-out/

# Clés Stripe de test, posées à la main, jamais commitées (SPEC-004)
.env.stripe.local

# Variables locales de l'api (clé Resend, etc.), lues par `pnpm --filter bookparking-api start`.
# Jamais commitées (SPEC-006). `.env.example`, lui, reste suivi.
.env
